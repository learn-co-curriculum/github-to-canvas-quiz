# frozen_string_literal: true

RSpec.describe Github::To::Canvas::Quiz do
  it "has a version number" do
    expect(Github::To::Canvas::Quiz::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
